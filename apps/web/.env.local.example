# Copy to .env.local for `pnpm dev` runs.
# These point the BFF at the .NET API running outside docker (dotnet run).

API_INTERNAL_URL=http://localhost:5050
NEXT_PUBLIC_API_URL=http://localhost:5050

# Plain HTTP locally — keep cookies non-Secure so the browser stores them.
COOKIE_SECURE=false
